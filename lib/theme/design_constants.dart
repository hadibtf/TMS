import 'package:flutter/material.dart';

// App Colors - Updated to match the new design
class AppColors {
  static const Color primary = Color(0xFF2C3E50); // Dark Slate Blue
  static const Color accent = Color(0xFFE67E22); // Vibrant Orange
  static const Color background = Color(0xFF2C3E50); // Dark background for the timing screen
  static const Color lightBackground = Color(0xFFECF0F1); // Light grey for station list
  static const Color textPrimary = Colors.white;
  static const Color textPrimaryDark = Color(0xFF2C3E50);
  static const Color textSecondary = Colors.white70;
  static const Color cardBackground = Colors.white;
  static const Color divider = Color(0xFFBDC3C7);
}

// Text Styles
class AppFontSizes {
  static const double sm = 14.0;
  static const double md = 18.0;
  static const double lg = 22.0;
  static const double xl = 48.0;
  static const double xxl = 32.0;
}

// Opacities
class AppOpacities {
  static const double faded = 0.6;
  static const double moreFaded = 0.4;
}

// Dimensions
class AppDimensions {
  static const double paddingSm = 8.0;
  static const double paddingMd = 16.0;
  static const double paddingLg = 24.0;
  static const double borderRadius = 12.0;
  static const double buttonHeight = 60.0;
}